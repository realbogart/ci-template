name: build

on: push

jobs:
  build:
    name: build the application
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build
        container: nixos/nix:2.23.0
        run: nix build
